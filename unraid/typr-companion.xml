<?xml version="1.0" encoding="utf-8"?>
<Container version="2">
  <Name>Typr-Companion</Name>
  <Repository>ghcr.io/max-prime-math/typr-server:latest</Repository>
  <Registry>https://github.com/max-prime-math/typr-server/pkgs/container/typr-server</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/max-prime-math/typr-server/issues</Support>
  <GitHub>https://github.com/max-prime-math/typr-server</GitHub>
  <ReadMe>https://github.com/max-prime-math/typr-server/blob/main/docs/companion-unraid.md</ReadMe>
  <Project>https://typr.ca</Project>
  <Overview>Optional native LaTeX, TeXpresso, scoped workspace, and authenticated management service for Typr. Stateless by default; one dedicated workspace can be mapped explicitly. Trusted LAN/VPN use only; never expose it publicly.</Overview>
  <Description>Typr Companion is installed separately from the browser-local Typr PWA. It runs native latexmk/pdfLaTeX and experimental TeXpresso. Its separately authenticated management GUI shows services and activity and manages service users/API keys. On stock Unraid kernels without Landlock, the template permits an explicit stateless fallback only while no host workspace is mounted; use mutually trusted documents only. Browser projects remain authoritative. A mapped workspace still requires a working Landlock launcher and otherwise fails closed. No broad share or appdata root belongs here. The service API starts without required API keys for compatibility and must never be exposed to the public Internet.</Description>
  <Category>Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:8485]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/max-prime-math/typr-server/main/unraid/typr-companion.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/max-prime-math/typr/main/public/icons/icon-512.png</Icon>
  <ExtraParams>--restart=unless-stopped --user=1000:1000 --read-only --tmpfs=/tmp:rw,nosuid,nodev,noexec,size=536870912 --cap-drop=ALL --security-opt=no-new-privileges:true --pids-limit=256 --memory=2g --memory-swap=2g --cpus=2</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <Date>2026-08-19</Date>
  <Changes>2026-08-19: Add the separately authenticated management GUI port for service visibility, activity, users, and API keys. Preserve the volume-free stateless fallback for stock Unraid kernels.</Changes>
  <Beta>False</Beta>
  <Requires>Use only on a trusted LAN or private VPN with mutually trusted documents. Never expose either port to the public Internet. Choose a unique management password of at least 24 characters; use trusted HTTPS when the management network is not physically trusted. Remote Typr use requires a client-trusted HTTPS reverse proxy with WebSocket support and firewall/VPN restriction. For Tailscale, keep the container's Use Tailscale switch off and use host-level Tailscale Serve; never use Funnel. Stock Unraid may use the explicit volume-free stateless fallback; any mapped workspace requires working Landlock support and otherwise fails closed. On stock fallback deployments, management users/API keys are session-only and reset when the container restarts.</Requires>
  <Config Name="Companion port" Target="8484" Default="8484" Mode="tcp" Description="Host port used by the reverse proxy and local status checks. Container port must remain 8484." Type="Port" Display="always" Required="true" Mask="false">8484</Config>
  <Config Name="Management GUI port" Target="8485" Default="8485" Mode="tcp" Description="Host port for the management GUI. Container port must remain 8485. Keep it restricted to a trusted LAN/VPN and never expose it publicly." Type="Port" Display="always" Required="true" Mask="false">8485</Config>
  <Config Name="Management listen address" Target="TYPR_COMPANION_MANAGEMENT_HOST" Default="0.0.0.0" Mode="" Description="Required container listen address for the mapped GUI port. Keep 0.0.0.0 in bridge mode; remote mode refuses startup without a strong management password." Type="Variable" Display="advanced" Required="true" Mask="false">0.0.0.0</Config>
  <Config Name="Management password" Target="TYPR_COMPANION_MANAGEMENT_PASSWORD" Default="" Mode="" Description="Required administrator password for the GUI; at least 24 characters. The browser username is typr. This is separate from service API keys." Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="Allowed Typr origins" Target="TYPR_COMPANION_ALLOWED_ORIGINS" Default="https://typr.ca,https://beta.typr.ca,https://dev.typr.ca" Mode="" Description="Comma-separated exact browser origins, including scheme, host, and port with no path or trailing slash. Add the self-hosted Typr origin. CORS is not authentication." Type="Variable" Display="advanced" Required="true" Mask="false">https://typr.ca,https://beta.typr.ca,https://dev.typr.ca</Config>
  <Config Name="Allow stateless Unraid fallback" Target="TYPR_COMPANION_ALLOW_UNSANDBOXED_STATELESS" Default="1" Mode="" Description="Keep exactly 1 on stock Unraid: if Landlock is unavailable, allow trusted-document compilation only when no host workspace is mounted. A mapped workspace always fails closed without Landlock." Type="Variable" Display="advanced" Required="true" Mask="false">1</Config>
  <Config Name="Workspace directory" Target="/workspace" Default="" Mode="rw" Description="Optional one exact dedicated project directory. Never map /, /mnt, /mnt/user, a broad share, appdata root, symlinked root, or the Docker socket. UID 1000 needs read/write access." Type="Path" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="Workspace API root" Target="TYPR_COMPANION_WORKSPACE_ROOT" Default="" Mode="" Description="Set exactly /workspace only when Workspace directory is configured. Leave blank to disable the file API." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="Workspace ID" Target="TYPR_COMPANION_WORKSPACE_ID" Default="unraid-workspace" Mode="" Description="Stable opaque letters/numbers/dot/underscore/hyphen identifier for browser bindings; never put a host path here. Ignored while the API root is blank." Type="Variable" Display="advanced" Required="false" Mask="false">unraid-workspace</Config>
</Container>
